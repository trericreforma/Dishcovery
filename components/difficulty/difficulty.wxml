<view class="selector-container">
  <block wx:for="{{options}}" wx:key="index">
    <view
      class="pill {{selected === item.value ? 'pill-active' : ''}}"
      data-value="{{item.value}}"
      bindtap="onSelect"
    >
      {{item.label}}
    </view>
  </block>
</view>
