<view class="floating-menu">
  <!-- Home -->
  <view class="menu-item" bindtap="goHome">
    <image src="../../assets/icons/home{{activePage === '/pages/index/index' ? '_active' : ''}}.png" class="menu-icon" />
  </view>

  <!-- Search -->
  <view class="menu-item" bindtap="goSharedRecipes">
    <image src="../../assets/icons/share{{activePage === '/pages/sharedRecipes/sharedRecipes' ? '_active' : ''}}.png" class="menu-icon" />
  </view>

  <!-- Center FAB (plus button) -->
  <view class="fab" bindtap="onAdd">
    <text class="fab-icon">＋</text>
  </view>

  <!-- Bookmark -->
  <view class="menu-item" bindtap="goBookmark">
    <image src="../../assets/icons/bookmark{{activePage === '/pages/bookmarks/bookmarks' ? '_active' : ''}}.png" class="menu-icon" />
  </view>

  <!-- Profile -->
  <view class="menu-item" bindtap="goMyRecipes">
    <image src="../../assets/icons/my-recipes{{activePage === '/pages/my-recipes/my-recipes' ? '_active' : ''}}.png" class="menu-icon" />
  </view>
</view>
