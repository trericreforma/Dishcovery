<view class="btn-wrap {{block ? 'block' : ''}}">
  <view 
    class="pill-btn {{type === 'primary' ? 'pill-primary' : 'pill-secondary'}} {{disabled ? 'pill-disabled' : ''}}"
    catchtap="onTap"
    data-disabled="{{disabled}}"
  >
    <text class="btn-label">{{label}}</text>
  </view>
</view>
